<%=render 'menus/sticky' %>
<div class="container-fluid">
        <div class="row">
    <div class="col-lg-12 col-12">
<%= form_with(model: creneau, local: true) do |form| %>
  <% if creneau.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(creneau.errors.count, "error") %> prohibited this creneau from being saved:</h2>

      <ul>
      <% creneau.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group col-lg-4">
    <%= form.label :titre %>
    <%= form.text_field :titre, class:"form-control" %>
  </div>

    <div class="form-group col-lg-6">
    <%= form.label :heure_de_début %>
    <%= form.time_field :heure_debut, :value => Time.now.in_time_zone('Europe/London').formatted_offset.strftime('%H:%M') , class:"form-control", readonly: true %>
  </div>

    <div class="formgroup col-lg-6">
    <%= form.label :heure_de_fin %>
    <%= form.time_field :heure_fin, :value => Time.now.strftime('%H:%M'),  class:"form-control", readonly: true  %>
  </div>

  <div class="form-group col-lg-12">
    <%= form.submit "Commencer", class:"btn btn-outline-danger" %>
  </div>
<% end %>
</div>
</div>
</div>